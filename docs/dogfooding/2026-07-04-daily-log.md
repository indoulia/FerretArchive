ID: N/A
Title: Dogfooding Daily Log — 2026-07-04
Type: Reference
Status: Active
Version: 1.0
Owner: TODO
Approved By: TODO
Related Documents: [2026-07-04-session-plan.md](2026-07-04-session-plan.md)
Last Updated: 2026-07-04

---

# Dogfooding Daily Log — 2026-07-04

- Machine / OS: Windows 11 Pro
- Ferret version: 0.16.0+f7abf07bde8723c3df770aa9ddb479b19cbc702c
- Install source: published npm (`@indoulia/ferret`)
- Phase(s) exercised: 1 (Personal Workflow), 6 (Benchmark Collection, partial)

## What happened, in order

1. **Test suite baseline** — `dotnet test src/Ferret.sln`: 0 failures, 652 passed / 5 skipped (pre-existing skips: 1 perf-benchmark opt-out, 4 OpenAI live-API tests). See `2026-07-04-session-plan.md` for the full per-project table.
2. **`ferret doctor` mojibake — investigated, ruled out.** Garbled icons appeared in Git Bash; confirmed via native PowerShell (UTF-8 codepage 65001) that they render correctly (✓). Terminal artifact, not a Ferret defect. Not filed.
3. **`ferret --version` cold-start:** 1.282s real time (Git Bash overhead included). DOGFOOD-001's Phase 6 target is ≤1s — this run misses it, though process-spawn overhead from the shell wrapping the call isn't isolated out. Logged as a data point, not yet a confirmed regression.
4. **`ferret index` — real bug found, then corrected.** Full run reported `Discovered: 1522, Indexed: 0, Skipped: 1237, Failed: 285`, exit code 1, with all 285 failures being directories hitting `UnauthorizedAccessException`. Traced to `FilesystemConnector.OpenAsync` (`src/Ferret.Connectors.Filesystem/FilesystemConnector.cs:76-84`) calling `File.OpenRead` on `AssetKind.Directory` assets with no kind check.
   - Filed as [GitHub issue #13](https://github.com/indoulia/Ferret/issues/13).
   - **Correction:** further investigation found `IndexPipeline.cs` on current `main` (lines 94-100) already skips non-`File` assets — the fix (commit `e5f344b`, PR #10) is already merged to `main`. `git merge-base --is-ancestor e5f344b f7abf07` confirmed the fix is **not** an ancestor of the commit the installed npm v0.16.0 build came from. So this is not an open code defect — it's the published npm release lagging an already-merged fix. Updated issue #13 with this correction rather than opening a redundant fix branch.
5. **`ferret search` — works correctly.** Queries against this repo returned accurate, relevant results (e.g. `docs/pkm/PKM-Governance.md`, `DependencyGraphMaterializer.cs`), including content from files committed within the last hour — meaning a background `ferret` process (PID 29620, running since 2026-07-03) is keeping the index fresh independent of the manual `ferret index` run above.
6. **Component test-coverage spot check (TokenSave-based, not exhaustive).**
   - Project-level: every `src/` project has a matching `*.Tests` project; `Ferret.VerticalSlice` is covered via `Ferret.Integration.Tests` rather than a dedicated project — not a gap.
   - `FilesystemConnector.cs`: 4/8 production functions untested by any transitive test path. Three are trivial (constructor, `ConnectAsync`, `DisconnectAsync`). **`GetHealthAsync` (line 41) is a real gap** — it has three real branches (path-missing, `IOException`, `UnauthorizedAccessException`) with zero test coverage.
   - Self-correction mid-survey: queried coverage for `RootCommandFactory.Build`, a root CLI wiring hotspot — its transitive coverage is nearly the entire test suite (60+ tests shown, truncated), which is not a useful signal and cost more tokens than it was worth. Stopped the survey there rather than continuing down that path.
7. **Repo reorganization** (per request, not a dogfooding finding): consolidated `docs/DOGFOOD-001.md`, `docs/DOGFOOD.md`, and this session's plan/log into `docs/dogfooding/`. Updated the one live reference (ADR-0025); left historical sprint-14 planning docs' mentions of the old path untouched as historical record.
8. **Branch hygiene:** consolidated three fragmented branches (a stray fix branch, a docs-reorg branch, and the original plan branch) into a single `dogfooding` branch per instruction, pushed to origin.
9. **Sample data located:** `C:\POC\FerretTest\App1` / `App2` (11 PDFs each) plus a pre-existing, well-built A/B benchmark harness (`Benchmarking/Run-FerretBenchmark.ps1`) with dated results from 2026-07-01 already validating Ferret's PDF-reading advantage over a no-Ferret baseline. Not yet re-run this session (real API cost — see plan for the cost-conscious approach).

## Went well

- Caught two things that would have wasted real effort if I hadn't verified first: the doctor "bug" (terminal artifact, not real) and the index "bug" (already fixed on `main`, just not yet released) — both confirmed by evidence, not assumption, before acting on them.
- Precise root-cause trace to file:line for the index issue, including a definitive `git merge-base` check rather than guessing at commit ordering from timestamps (which would have been misleading here).
- Zero fabricated data — every number above is from an actual command run.

## Didn't go well

- Branch/file organization was sloppy at first: committed directly to local `main` once (caught and fixed before push), then created two more fragmented branches before consolidating into one per instruction. Should have asked "one branch for all of this, or per-item?" before starting rather than after.
- One token-wasteful tool call (root CLI wiring hotspot's transitive coverage) that should have been anticipated as low-signal before running it.
- Initially used the wrong GitHub label name (`dogfood` vs. the actual established `dogfooding`) — created and then had to delete a duplicate label.

## Needs improvement

- Cold-start timing (1.282s vs ≤1s target) needs a cleaner measurement — isolate shell/process-spawn overhead from the binary's own startup cost before treating it as a real regression.
- No daily-log entries existed anywhere in the repo before today, despite DOGFOOD-001 being active since 2026-06-30 — this is the first one.

## Missing / not yet done

- Live A/B benchmark re-run (real $ cost, holding for a cost checkpoint — see plan).
- App1/App2 real-PDF dogfooding (indexing, search/ask quality against real documents).
- Full Phase 6 benchmark table (throughput, incremental re-index time, peak memory, index-size ratio).
- Broader component-test-coverage survey beyond the two files spot-checked here.
