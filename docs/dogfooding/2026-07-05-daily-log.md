ID: N/A
Title: Dogfooding Daily Log — 2026-07-05
Type: Reference
Status: Active
Version: 1.0
Owner: TODO
Approved By: TODO
Related Documents: [2026-07-04-daily-log.md](2026-07-04-daily-log.md), [2026-07-04-session-plan.md](2026-07-04-session-plan.md)
Last Updated: 2026-07-05

---

# Dogfooding Daily Log — 2026-07-05

- Machine / OS: Windows 11 Pro
- Ferret version (installed CLI): 0.16.0+f7abf07bde8723c3df770aa9ddb479b19cbc702c
- Repositories used: Ferret (primary), one external ~27k-file repository (secondary, read-only)
- Phase(s) exercised: 1 (Personal Workflow), 2 (Daily AI Workflow — investigation), 3 (Multi-Repository Validation)

## Real task performed

Investigated a real dependency question (an IdentityModel version decision already flagged in the external repo's own files) using `ferret search` as the primary tool, per the "do real work, not synthetic testing" operating mode.

## Findings

1. **Bug — `ferret search` rejects all hyphenated queries.** `ferret search "nem-3795"` and `ferret search "test-hyphen"` both failed with `Invalid query: empty or whitespace` — a misleading message, since the input was neither. Root cause traced: `QueryTranslator.EscapeKeyword` treated `-` as safe/unquoted, but SQLite FTS5's query grammar treats a bare `-` as its NOT operator (`nem-3795` parsed as `nem AND NOT 3795`); the resulting provider failure's diagnostics were then silently dropped in `SearchService.SearchAsync`, so the CLI fell back to its generic empty-query message. Filed as [#15](https://github.com/indoulia/Ferret/issues/15), **fixed via TDD** (commit `366cc3d`): quote hyphenated keywords like any other special character. Full suite green.
2. **Bug — `packages/` (NuGet cache) not in the default indexing skip list.** Searching the external repo for a real dependency question returned 20 results, **all vendored package XML doc-comments**, drowning out the actual hand-authored analysis document, which never appeared. `node_modules`/`bin`/`obj` were already excluded; `packages` (the equivalent for classic-style .NET projects) was not. Filed as [#16](https://github.com/indoulia/Ferret/issues/16), **fixed via TDD** (commit `bedbc94`): added `packages` to `HardcodedSkipDirs`. Full suite green.
3. **Verified fix completeness for yesterday's #14** using `ferret search "AbsolutePath"` on this repo itself — confirmed both fixed call sites now show `Uri.UnescapeDataString(...)` in the indexed content, and no other production code path has the same unescaping gap (other `AbsolutePath` hits are unrelated HTTP-URI contexts). Real, useful validation loop: the tool being fixed was used to verify its own fix.
4. **Documentation gap (not fixed, flagged for review):** `docs/dogfooding/DOGFOOD-001.md` Phase 2 (lines 82, 84) instructs using `ferret ask`, which does not exist as a CLI command — confirmed via `ferret --help`. Closest real equivalent is `ferret context <query>`. Not corrected here: it's a governance document, out of scope for me to edit without authorization.

5. **Performance finding — `ferret watch` search-visibility latency far exceeds the ≤5s target (not fixed, no confident root cause).** First real exercise of `watch` this session: started it, saved a file with a distinctive marker, polled `ferret search` for it. The index DB file was touched only 0.64s after the save (fast), but the content was still not searchable after 27s of polling; it did eventually become searchable, but the exact crossover time wasn't captured (polling stopped at 27s; a later manual retry succeeded). `FileChangeDebouncer`'s configured window is only 500ms, so it can't explain the gap alone. Filed as [#17](https://github.com/indoulia/Ferret/issues/17) with the measurement caveats stated plainly — **not fixed**, since I don't have a confident root cause to act on (per the "don't guess a fix" rule). Process hygiene note: had two throwaway `ferret watch` background processes running mid-investigation; verified each one's exact PID via its command line and creation time before stopping it, after a blocked attempt to force-kill an unverified PID.

6. **Reliability finding — index goes stale across a branch switch with zero warning.** Real task: switched `dogfooding` → `main` (no reindex, as most people naturally wouldn't remember to) → same search still returned `dogfooding`-only content unchanged → `ferret doctor` showed no freshness warning (its check is purely time-based, no git-branch awareness). Not an obvious one-line fix (needs new state: track git HEAD at index time, decide how to surface a mismatch) — filed as [#18](https://github.com/indoulia/Ferret/issues/18), not implemented, per "open an issue when uncertain rather than guess." Reindexed afterward to restore correct state on `dogfooding` (Discovered jumped 1522→3060, consistent with #13/#16 not being in the installed binary plus extra `bin`/`obj` churn from today's `dotnet test` runs — not a new issue).

7. **Repeated-query check — no defect.** Same query run 4× consecutively: 63/70/76/81ms, flat and predictable, no degradation pattern. No result-caching layer, but base latency is already well within target, so not filing anything.

8. **`ferret context` help-text default mismatch — fixed.** `--help` showed `[default: 0]` for `--max-tokens`/`--max-documents` vs. documented 8000/10; runtime was already correct. Traced to missing `DefaultValue` on `OptionDefinition` in `ContextCliModule.cs`. Fixed, regression test added, full suite green. Filed [#19](https://github.com/indoulia/Ferret/issues/19) (already resolved). Initially suspected a severe "0 documents returned" bug — verified first, was my own query-phrasing mistake, not filed.

9. **MCP tool sweep — real code-review work, not synthetic.** After fixing #19, deliberately checked whether adjacent MCP tools shared the same class of defect:
   - `SearchTool`: **found a real, independent bug** — checked only `Hits.Count == 0`, never `IsSuccess`, so any search failure (invalid query, missing index, missing workspace) was misreported to MCP/AI callers as generic "No results found." Filed and **fixed** as [#20](https://github.com/indoulia/Ferret/issues/20) (TDD, full suite green).
   - `ContextAssembler` (backs both `ferret context` and the MCP `ferret_context` tool): correctly checks `IsSuccess`, but silently drops a failed search to an empty hit list with **no error signal at all** — `ContextPackage` has no status/error field. Filed as [#21](https://github.com/indoulia/Ferret/issues/21), **not implemented** — fixing it means changing a public contract (new field, exception behavior, or Result-wrapper), a real design decision, not an obvious correction.
   - `ContextTool`, `ReadDocumentTool`, `WorkspaceStatusTool`: checked, clean, no defect.
10. **Foreign commit noticed on this branch:** `7dac324` ("chore: ignore .worktrees/ directory", timestamp 00:23:34) was not authored by me in this session — it's the other concurrent agent's worktree-setup commit landing on whatever branch was checked out on disk (`dogfooding`) at that moment. Not reverted; noted for visibility.

11. **Reference-discovery task using `ferret search` as primary tool — found and fixed a High-severity systemic bug.** Used `ferret search "_searchService.SearchAsync"` to enumerate every consumer of the search service (confirming #20/#21 coverage was complete: 4 real call sites, all accounted for). While inspecting raw `--format json` output, noticed every hit's `canonicalUri` was malformed: `"file:///filesystem:///..."` — a double-wrapped scheme, on 100% of results. Traced to `Bm25SearchProvider.BuildHit`: a `StartsWith("file://")` check that never matches real `"filesystem://"`-scheme IDs (diverge at char 5), so every already-absolute ID gets wrongly re-wrapped. Compounding factor: the test fixture's own seed data used the wrong `'file://'` scheme, hiding the bug from the suite entirely. Fixed both (real fix + realistic test data), filed and closed as [#22](https://github.com/indoulia/Ferret/issues/22). Full suite green.

12. **GitHub issue hygiene.** Found #13 duplicates a pre-existing #9 (same bug, filed 2026-06-01, before this session) — closed #13 as duplicate, consolidated the fix-status comment onto #9, left #9 open (fix on `main`, not yet released). Closed #14/#15/#16/#19/#20/#22 with commit references and an explicit "committed to `dogfooding`, not pushed/merged" caveat on each, so the closed status doesn't imply the fix has shipped. Left #17/#18/#21 open (correctly — not implemented).

13. **`--passages` flag is fully non-functional.** Documented as "Return passage-level results instead of files"; verified byte-for-byte identical JSON output with/without it. `grep` confirms `SearchOptions.IncludePassages` is set from the CLI but read nowhere in the search pipeline — the `PassageSearchHit` data model exists (with its own tests) but nothing produces one. Real feature work, not an obvious fix — filed as [#23](https://github.com/indoulia/Ferret/issues/23), not implemented.

14. **Real trust test: "could I self-serve configure an AI provider using Ferret's own docs?" — No.** Found a genuine 3-way config-filename split baked into the source itself, not just docs: the app's actual startup path (`RootCommandFactory.cs` → `FerretConfigLoader.Load(null)`) defaults to `ferret.json`; `ferret config validate` independently defaults to `ferret.config.json`; a hardcoded CLI message says `.ferret/config.json`. 12 Manual pages (21 occurrences) agree with the validate command's `ferret.config.json`, not with what the app actually loads. This means `ferret config validate` can report a config "valid" while the running app silently never reads it. Release-Blocker-severity Architecture Discovery — filed as [#24](https://github.com/indoulia/Ferret/issues/24), **not implemented** (picking a canonical filename is a real decision with backward-compat implications, not a typo fix).

15. **`ferret status` is a hardcoded stub — always says "not running."** Real workflow test: `ferret start` (background) → confirmed genuinely alive via verified PID → `ferret status` → still "not running." Source confirms: `StatusCommandHandler.cs` is 3 unconditional lines, comment says "Sprint 6: no IPC. Sprint 7 adds named-pipe health endpoint" — a known, never-finished placeholder, locked in by its own test (`Status_ReportsNotRunning_ExitsOne`). Product Completeness Gap, High — filed as [#25](https://github.com/indoulia/Ferret/issues/25), not implemented (needs real IPC design).

16. **Real connector lifecycle test (enable → inspect → list → disable) — found and fixed another hardcoded-false bug.** `inspect` correctly showed a newly enabled instance; `list`'s `CONFIGURED` column stayed "no" regardless. Traced to `ConnectorListCommandHandler.cs`: `IsConfigured: false` hardcoded, never checking `IConnectorInstanceStore` — same class of defect as `ferret status` (#25), but this one had a directly reusable fix pattern already established elsewhere (`ConnectorValidateCommandHandler`), so fixed it (commit `46d7092`). Zero prior test coverage existed for this handler; added `ConnectorListCommandHandlerTests.cs` from scratch. Filed and closed as [#26](https://github.com/indoulia/Ferret/issues/26). Full suite green. Cleaned up the test instance afterward (disabled, not deleted).

17. **Most serious finding of the session: my own earlier connector enable/disable test (item 16) silently broke indexing for the entire workspace.** Attempting a rename-file test, `ferret index` returned `Discovered: 0, Indexed: 0, Failed: 0` at 0.05s for a 3000+ file repo — a "successful"-looking output hiding total failure. Root-caused to `ConnectorManager.cs`'s zero-config synthetic default, which is keyed on the instance store being *empty*, not on "any instance enabled" — confirmed via its own code comment as intentional design. Once I created (then disabled) `test-instance` (item 16), the store was no longer empty, the synthetic default permanently stopped applying, and with zero enabled instances, nothing was ever indexed again — no error, no warning, at any step. There is also no `connector remove` command — the only recovery I found was hand-editing `.ferret/connectors.json` back to `{"instances": []}`, which I did to restore my own workspace (confirmed: 3074 discovered, 2492 indexed, search working again). Release Blocker — filed as [#27](https://github.com/indoulia/Ferret/issues/27), not implemented (real design decision: rekey the default condition, add active-connector warnings, and/or add a `remove` command).

18. **Rename-file real workflow test — found and fixed a stale-index bug.** Renamed a real test file, reindexed twice: search kept returning the deleted old path as a phantom duplicate, permanently. Traced to `IndexPipeline.cs`'s stale-asset cleanup calling only `_stateStore.RemoveAsync` (bookkeeping), never the already-existing, already-tested `IIndexEngine.DeleteAsync` (actual index removal). Fixed via TDD (commit `85e78a6`), filed and closed as [#28](https://github.com/indoulia/Ferret/issues/28). Full suite green.

19. **Fixed 3 more issues per explicit "fix them all" instruction:** #27 (connector zero-config trap — changed synthesis condition from `instances.Count == 0` to `instances.All(i => !i.IsEnabled)`, verified backward-compatible against all 6 pre-existing tests by tracing each), #24 (config filename — chose `ferret.json` as canonical since it's what the app actually loads, aligned `config validate`, CLI messages, and 21 occurrences across 12 Manual pages to match, added 2 regression tests). #17/#18/#21/#23/#25 remain open — genuine design decisions or unconfirmed root causes, not attempted per the "don't guess, don't build new features" rule. All fixes TDD, full suite green throughout.
20. **Created `REGRESSION-CHECKLIST.md`** (per request) — a start-of-session checklist future dogfooding passes should run first, covering every real check performed across both days' sessions, before exploring new territory.

## Went well

- All three real findings today came from genuinely trying to answer a real question, not from synthetic probing — exactly the intended dogfooding mode.
- Caught the misleading-error-message pattern (diagnostics dropped in `SearchService.cs`) as a contributing factor, not just the surface symptom, without over-scoping into fixing that secondary issue in the same change.
- Both fixes were small (1-2 line), well-evidenced by a failing test first, and verified with a full-suite regression run before committing.

## Didn't go well / needs improvement

- Attempted `npx -y tokensave sync` to refresh a stale code-graph index — correctly blocked (auto-installing a guessed package name isn't safe); worked around it by searching directly instead, which was sufficient.
- The `ferret ask` documentation gap has sat unflagged since DOGFOOD-001 was authorized (2026-06-30) — first surfaced today, five days in.

## Missing / not yet done

- Live A/B benchmark re-run (still holding for a cost checkpoint).
- Full Phase 6 benchmark table.
- A clean (non-degraded) throughput baseline on the external repo, now blocked on releasing #13/#14/#15/#16.
