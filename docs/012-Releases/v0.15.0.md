# Ferret v0.15.0 — Distribution Platform

**Ferret is now distributed as a self-contained application. Install it without cloning the repository, building from source, or installing the .NET SDK.**

```bash
npm install -g @indoulia/ferret
ferret --version
```

## Why this release matters

Previous Ferret releases were primarily intended for developers building from source. v0.15.0 introduces the **Distribution Platform**, making Ferret installable and consumable as a packaged product while establishing **GitHub Releases as the single source of truth** for all future distribution channels.

## What's new

- Self-contained cross-platform binaries
- GitHub Releases as the distribution source
- Versioned `release-manifest.json` contract
- NPM installation (`@indoulia/ferret`)
- SHA256 verification
- Atomic installation
- Safe uninstall that preserves user data

## Highlights

- **GitHub Releases are the single source of truth.** Every distribution channel consumes published release assets through a stable, versioned `release-manifest.json` contract — no second build, no filename guessing.
- **NPM is the first distribution channel built on the Distribution Platform.** The wrapper downloads the correct self-contained binary for your platform, verifies its SHA256, and installs it atomically. One runtime dependency (`extract-zip`); everything else is the Node standard library.
- **Checksum-verified, atomic installs.** Downloads are verified against the manifest's SHA256 (hard-fail on mismatch) and swapped into place so a failed install never destroys a working one.
- **Your data is never touched on uninstall.** `npm uninstall -g @indoulia/ferret` removes only the binary; `.ferret` workspaces, indexes, and configuration are left intact.

## Install / Upgrade / Uninstall

| Action | Command |
|---|---|
| Install | `npm install -g @indoulia/ferret` |
| Upgrade | `npm update -g @indoulia/ferret` |
| Uninstall | `npm uninstall -g @indoulia/ferret` |

Install locations: Windows `%LOCALAPPDATA%\Programs\Ferret` · macOS `~/Library/Application Support/Ferret` · Linux `~/.local/share/ferret`.

## Assets in this release

- `Ferret-0.15.0-win-x64.zip`, `Ferret-0.15.0-linux-x64.zip`, `Ferret-0.15.0-osx-arm64.zip`, `Ferret-0.15.0-osx-x64.zip` — self-contained, single-file binaries.
- `SHA256SUMS.txt` — checksums for all archives.
- `release-manifest.json` — the machine-readable distribution contract (`schemaVersion: 1`).
- `*.nupkg` — NuGet packages (unchanged).

Verify a download manually:

```bash
sha256sum -c SHA256SUMS.txt
```

## For maintainers of other channels

Read `release-manifest.json` to resolve assets — do not infer filenames or scrape the release page. The same contract is reserved for future consumers (Homebrew, winget, Chocolatey, Scoop, an enterprise mirror, and `ferret self-update`). **These future channels will consume the same `release-manifest.json` contract without requiring changes to the release pipeline.**

## Known limitations

- **macOS binaries are unsigned and unnotarized.** macOS users will see a Gatekeeper prompt; allow the binary in System Settings → Privacy & Security. Signing is deferred to a future milestone.

## Architecture

```text
Git Tag
    ↓
GitHub Release
    ↓
release-manifest.json
    ↓
NPM Wrapper
    ↓
Ferret Binary
```

See `docs/002-Architecture/ARCH-022-Distribution-Platform-Architecture.md` for the full design.
